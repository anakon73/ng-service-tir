import { Meta, StoryObj } from '@storybook/angular'

import { Navbar } from '.'

const meta: Meta<Navbar> = {
  title: 'features/Navigation/Navbar',
  component: Navbar,
}

export default meta

export const Default: StoryObj<Navbar> = {}
