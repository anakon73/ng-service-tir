import { Meta, StoryObj } from '@storybook/angular'

import { NFooter } from '.'

const meta: Meta<NFooter> = {
  title: 'shared/ui/NFooter',
  component: NFooter,
}

export default meta

export const Default: StoryObj<NFooter> = {}
