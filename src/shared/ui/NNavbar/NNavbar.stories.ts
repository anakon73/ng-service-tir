import { Meta, StoryObj } from '@storybook/angular'

import { NNavbar } from '.'

const meta: Meta<NNavbar> = {
  title: 'shared/ui/NNavbar',
  component: NNavbar,
}

export default meta

export const Default: StoryObj<NNavbar> = {}
